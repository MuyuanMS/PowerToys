<ContentDialog
    x:Class="Microsoft.PowerToys.Settings.UI.Views.PowerDisplayWarningDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <StackPanel Spacing="12">
        <controls:InfoBar
            x:Name="WarningInfoBar"
            IsClosable="False"
            IsOpen="True"
            Severity="Warning" />
        <TextBlock x:Name="WarningDescription" TextWrapping="WrapWholeWords" />
        <ItemsControl x:Name="WarningList" Margin="20,0,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Spacing="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}" TextWrapping="WrapWholeWords" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <HyperlinkButton
            x:Name="LearnMoreLink"
            Margin="-12,0,0,0"
            AutomationProperties.AutomationId="PowerDisplayWarningLearnMoreLink" />
    </StackPanel>
</ContentDialog>
